// arquivo para validações especiais. já deixar pronto para o login

import { AbstractControl } from '@angular/forms';
/*
export class Validacoes(){
  // relacionado a senha
 static SenharCombinam(controle : AbstractControl){
    // pegar os valores do campo
    let senha = controle.get('senha').value;
    let confirmarSenha = controle.get('confirmarSenha').value;

    // verificação
    if (senha === confirmarSenha) return null;

    controle.get('confirmarSenha').setErrors({ senhasNaoCoincidem: true });

  }


}
*/
